const menu=document.querySelector('.navtoggle');const nav=document.querySelector('.nav');if(menu)menu.addEventListener('click',()=>{nav.classList.toggle('open');menu.setAttribute('aria-expanded',nav.classList.contains('open'))});
document.querySelectorAll('.lead-form').forEach(form=>form.addEventListener('submit',async e=>{e.preventDefault();const out=form.querySelector('.status');const endpoint=form.dataset.endpoint;if(!endpoint||endpoint.includes('PASTE_')){out.textContent='Form setup is not complete. Please call (613) 912-7508.';return}const button=form.querySelector('button');button.disabled=true;out.textContent='Sending…';try{await fetch(endpoint,{method:'POST',mode:'no-cors',body:new FormData(form)});form.reset();out.textContent='Thanks. Your request was sent. We will contact you soon.'}catch(err){out.textContent='We could not send this request. Please call (613) 912-7508.'}finally{button.disabled=false}}));
